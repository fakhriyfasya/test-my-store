pipeline {
    agent {label "linux"}

    stages {
        stage('Git Clone') {
            steps {
                git 'https://github.com/fakhriyfasya/test-my-store.git'
            }
        }    
    }
}
